import 'package:flutter/material.dart';

class FeedbackMolecule extends StatelessWidget {
  const FeedbackMolecule({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}