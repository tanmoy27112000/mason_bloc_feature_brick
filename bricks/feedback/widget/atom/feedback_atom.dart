import 'package:flutter/material.dart';

class FeedbackAtom extends StatelessWidget {
  const FeedbackAtom({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}